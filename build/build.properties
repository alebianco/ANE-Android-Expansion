# Project properties
project.name = ANE-Android-Expansion
version.num = 1.0.1
project.name.versioned=${project.name}-v${version.num}

extension.descriptor = ${basedir}/build/extension.xml

# Certificate
crt.file = ${basedir}/build/certificate.p12
crt.password = password

# Folders
tmp.dir = ${basedir}/temp
bin.dir = ${basedir}/bin
lib.dir = ${bin.dir}/libs
ext.dir = ${basedir}/source
doc.dir = ${basedir}/docs
dist.dir = ${basedir}/release

bin.def.dir = ${bin.dir}/native/default
bin.and.dir = ${bin.dir}/native/android

src.def.dir = ${ext.dir}/actionscript/default
src.as3.dir = ${ext.dir}/actionscript/agnostic
src.and.dir = ${ext.dir}/android/air_extension
